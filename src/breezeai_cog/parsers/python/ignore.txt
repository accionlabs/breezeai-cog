# Python per-language ignore defaults (layer 2).
# `env/` is scoped here (not the universal walk-time set): it's a virtualenv name that collides
# with non-Python trees (e.g. Terraform `terraform/env/`), so it must only exclude Python files.
env/
*.pyc
*.pyo
*.pyd
*.so
__pycache__/
*.egg-info/
.eggs/
.mypy_cache/
.pytest_cache/
